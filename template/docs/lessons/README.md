---
id: lessons-index
title: Lessons — bugs already paid for, and the rules that prevent them
status: active
owner: unassigned
last-verified: <YYYY-MM-DD>
---

# Lessons

One file per bug. Always three parts: **Symptom → Root cause → Rule.**

The goal is not to record history. It is to stop the same *class* of bug from recurring. **If a file here does not end in a rule someone can act on, it is not finished.**

## When to write one

After fixing a bug that is (a) not obvious in its cause, and (b) able to recur. A trivial bug with an obvious cause does not need a file.

## Why this folder earns its keep

The bugs that cost the most are rarely hard. They are the ones somebody already solved once, in a session nobody can find any more.
