---
name: The guard did the wrong thing
about: It failed on something valid, or passed something it should have caught
labels: bug
---

**What the guard said** (paste the output, or `GREEN` if that is the problem)

```text

```

**What you expected instead**

**Smallest reproduction.** A failing case in `tests/run.sh` shape is ideal:

```bash
# what you created, and what you ran
```

**Environment:** OS, `bash --version`, `git --version`
